TalkBack/Hello implemented as per spec. and works correctly,
TalkBack/Sort implemented as per spec. and works correctly,
User/New GET implemented as per spec. and works correctly,
Database implemented as per spec. Works safely, correctly and data access is loosely coupled,
Logging implemented as per spec. and works safely and correctly,
User/New POST implemented as per spec. and works correctly,
AuthorizationHandler Used Correctly,
AuthenticationHandler Used Correctly,
User/ChangeRole implemented as per spec. and works correctly,
User/RemoveUser implemented as per spec. and works correctly,
Protected/Hello implemented as per spec. and works correctly,
Protected/SHA1 implemented as per spec. and works correctly,
Protected/SHA256 implemented as per spec. and works correctly,
Protected/GetPublicKey implemented as per spec. and works correctly,
Protected/Sign implemented as per spec. and works correctly,
Protected/Mashify implemented as per spec. and works correctly,
Client uses async programming,
Client TalkBack Hello implemented as per spec. and works correctly against test server,
Client TalkBack Sort implemented as per spec. and works correctly against test server,
Client User Get <name> implemented as per spec. and works correctly against test server,
Client User Post <name> implemented as per spec. and works correctly against test server,
Client User Set <name> implemented as per spec. and works correctly,
Client User Delete implemented as per spec. and works correctly against test server,
Client User Role <username> <role> implemented as per spec. and works correctly against test server,
Client Protected Hello implemented as per spec. and works correctly against test server,
Client Protected SHA1 <message> implemented as per spec. and works correctly against test server,
Client Protected SHA256 <message> implemented as per spec. and works correctly against test server,
Client Protected Get PublicKey implemented as per spec. and works correctly against test server,
Client Protected Sign <message> implemented as per spec. and works correctly against test server,
Client Protected Mashify implemented as per spec. and works correctly against test server,
Exceptional report that addresses all of the points in the specification in detail in a concise and clear scientific manner that shows an exceptional understanding of the concepts covered and uses references where appropriate to back up points.
